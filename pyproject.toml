[tool.poetry]
name = "coursework5"
version = "0.1.0"
description = ""
authors = ["Sergey"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
